<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="24.0dip"
    android:paddingVertical="12.0dip"
    android:background="@color/oui_round_and_bgcolor">

    <TextView
        android:id="@+id/equalizer_preview_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/pref_title_equalizer_clear"
        android:textAlignment="viewStart"
        android:textSize="14sp"
        android:textColor="@color/bottom_tip_textview_text_color" />

</RelativeLayout>